package com.supertain.methods;

/*
 * 可变个数形参的方法
 * 
 * 1.jdk5.0新增的内容
 * 
 * 2.具体使用
 * 	> 格式: 数据类型 ... 变量名
 * 	> 当调用可变个数形参的方法时,传入的参数个数可以是0-∞个
 *  > 可变个数形参的方法与本类中的方法名相同,形参不同的方法之间构成重载
 *  > 可变个数形参的方法与本类中的方法名相同,形参类型也相同的数组之间不构成重载。二者不能共存
 *  > 可变个数形参在方法的形参中,必须声明在末尾,即最多只能声明一个可变个数形参
 */

public class MethodArgsTest {
	
}
